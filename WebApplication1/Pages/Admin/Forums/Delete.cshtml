@page
@model DeleteModel
@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>
<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Forum</h4>
    @if (!string.IsNullOrEmpty(Model.Icon))
    {
        <img src="@Model.Icon" class="img-thumbnail" alt="current_icon" width="48" height="48" />
    }
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Forum.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Forum.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Forum.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Forum.Description)
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Forum.ID" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index">Back to List</a>
    </form>
</div>
